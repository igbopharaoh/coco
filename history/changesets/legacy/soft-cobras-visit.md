---
'coco-cashu-core': patch
---

Add `manager.ops` as the standard operation API for send, receive, and melt flows, including the follow-up docs and typing updates for the default `bolt11` melt path.
